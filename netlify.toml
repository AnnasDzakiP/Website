[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Ini kunci untuk mematikan proteksi scanner yang terlalu sensitif
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY,NEXT_PUBLIC_MIDTRANS_CLIENT_KEY"